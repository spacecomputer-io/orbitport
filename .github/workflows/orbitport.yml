name: Test & Build Gateway 

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "*" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Install protoc
      run: |
        sudo apt-get update
        sudo apt-get install -y protobuf-compiler
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Format
      working-directory: ./orbitport
      run: cargo fmt -- --check
    - name: Clippy
      working-directory: ./orbitport
      run: cargo clippy -- -D warnings
    - name: Build
      working-directory: ./orbitport
      run: cargo build --verbose
    - name: Run tests
      working-directory: ./orbitport
      run: cargo test --verbose
